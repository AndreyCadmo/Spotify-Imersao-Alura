{
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "scripts": {
    "start": "vercel dev",
    "dev": "concurrently \"vercel dev\" \"json-server --watch api-artists/artists.json --port 3000\"",
    "build": "vercel"
  }
}
